# Базовый URL API. При пустом значении запросы идут на тот же хост, что и фронт (один сервер).
# Для локальной разработки с удалённым бэком раскомментируйте и укажите адрес:
# VITE_API_BASE_URL=http://185.239.50.252:8080
